# MOVED — this repo is now `rpoint-cs/` in champsim-infra

This project was subsumed into
[champsim-infra](https://github.com/rahulbera/champsim-infra) on 2026-08-18 as
the **`rpoint-cs/`** directory (rpoint = the capture-once/replay-many tracing
tool family; cs = ChampSim). The full history of this repo, with these commit
hashes, is preserved there as the second parent of the subsume merge.

This repo is archived read-only. Do not push here; nothing here will be
updated. The LLM cassettes under `artifacts/` live on in the new location.

